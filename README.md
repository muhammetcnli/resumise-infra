# resumise-infra

Local orchestration for Resumise services.

## Services
- postgres
- backend (Spring Boot)
- ai-api (Python)

## Prerequisites
- Docker Desktop
- Docker Compose v2

## 1) Setup env
Create `.env` from `.env.example` and fill secret values.

## 2) Run
```bash
docker compose up -d
